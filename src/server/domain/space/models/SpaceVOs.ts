export interface AddSpaceOptions {
    templateId: string;
    name?: string;
    parentId?: string;
    iconId: string;
    projectId?: string;
}

export interface EditSpaceOptions {
    name?: string;
    parentId?: string;
    iconId: string;
}
